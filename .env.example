# ---------------------------------------------------------------------------
# Mazora Network environment
# Everything is optional: with no vars set, the site runs on typed demo data.
# ---------------------------------------------------------------------------

# Public site URL (used for canonical URLs, sitemap, OpenGraph)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Database (Postgres connection string).
# Now: a Neon connection string, e.g. postgres://user:pass@ep-xxx.neon.tech/db?sslmode=require
# Later: swap for your Supabase Postgres connection string — no code changes needed.
# Leave empty to run entirely on in-memory demo data.
DATABASE_URL=

# Live Minecraft server status API (returns JSON). Leave empty to show
# "Server status temporarily unavailable" instead of fabricated numbers.
MINECRAFT_STATUS_API_URL=

# Shared secret the Minecraft plugin uses to POST account-link confirmations.
MINECRAFT_PLUGIN_SECRET=

# Discord invite (public)
NEXT_PUBLIC_DISCORD_INVITE_URL=https://discord.gg/example

# --- Phase 2 (Supabase auth/storage) — not required for Phase 1 ---
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
