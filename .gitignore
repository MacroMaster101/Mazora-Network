# Dependencies
node_modules
.pnp
.pnp.js

# Next.js build output
.next
.next-dev
/out
/build

# Production / misc build
/coverage

# Environment variables (never commit real secrets — .env.example is the template)
.env
.env.local
.env.*.local
.env.development.local
.env.test.local
.env.production.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Deployment
.vercel

# Certificates / keys
*.pem

# Editor / IDE
.vscode
.idea
*.swp

# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Local project docs (superpowers scratch)
/docs/superpowers
/.superpowers

# Local AI assistant workspaces and scratch state
/.codex
/.agents
/.agent
/.claude
/.cursor
/.aider
/.continue

# Tests are kept local and are not published to the repository.
# They still run with `npm test` on a machine that has them; a fresh clone
# will not, because these files are deliberately not distributed.
**/__tests__/
*.test.ts
*.test.tsx
*.spec.ts
*.spec.tsx
